# Minimal real schema so AppSync has resolvers.
# We'll replace/extend this with Price/Guess/UserState soon.

# Minimal placeholder schema).
# Replace with real domain types later.

type Query {
  noop: String
}

schema {
  query: Query
}
