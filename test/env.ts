/**
 * Shared test constants.
 *
 * Keep this file free of side effects (no process.env writes).
 * `jest.setup.ts` is responsible for seeding env vars.
 */

export const TEST_COGNITO_CLIENT_ID = 'test-client-id'
export const TEST_AWS_REGION = 'us-east-1'
export const TEST_AUTH_SECRET = 'test-auth-secret'
